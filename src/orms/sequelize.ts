
enum ORMNames {
    sequelize,
    typeorm,
    objection,
    mongodb,
    oracle
};

// Pass a file to Sequelize
const toSequelize = (fileName: Array<string>, orm: ORMNames) => {

    // convert to our standard and o

}

// pass
const fromSequelize = (fileName: Array<string>, orm: ORMNames) => {

}